import React from "react";
import styles from '../Logo/Logocs';
import {View, Image, Text} from 'react-native';
const logo = (props) => (
    <View style={styles.Logo1}>
      <Text>Image</Text>
    </View>
);

export default logo;